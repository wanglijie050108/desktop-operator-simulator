# AI Agent Instructions

Before analyzing, modifying, testing, debugging, integrating, configuring, or
operating this repository, read and follow:

```text
.trae/skills/human-operation-simulator-guardrails/SKILL.md
```

The Skill is mandatory for every repository task. In particular, create or update a
task record under `changelog/` from the start of the task and keep it synchronized
with meaningful analysis, operations, failures, decisions, validation, and results.

When instructions conflict, follow the user's latest explicit request, then this
file and the repository Skill, then the canonical documents listed by the Skill.
